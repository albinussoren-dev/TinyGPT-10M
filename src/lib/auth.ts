import { cert, getApps, initializeApp } from 'firebase-admin/app'; import { getAuth } from 'firebase-admin/auth'; import { prisma } from './prisma';
if (!getApps().length) initializeApp(process.env.FIREBASE_CLIENT_EMAIL ? {credential: cert({projectId:process.env.FIREBASE_PROJECT_ID, clientEmail:process.env.FIREBASE_CLIENT_EMAIL, privateKey:process.env.FIREBASE_PRIVATE_KEY?.replace(/\\n/g,'\n')})} : undefined);
export async function requireUser(req: Request){const token=req.headers.get('authorization')?.replace('Bearer ',''); if(!token) throw new Response('Unauthorized',{status:401}); const decoded=await getAuth().verifyIdToken(token); const user=await prisma.user.upsert({where:{firebaseUid:decoded.uid},update:{email:decoded.email??'',name:decoded.name},create:{firebaseUid:decoded.uid,email:decoded.email??'',name:decoded.name,role:'STUDENT'}}); return user;}
export async function requireAdmin(req: Request){const user=await requireUser(req); if(user.role!=='ADMIN') throw new Response('Forbidden',{status:403}); return user;}
